# Security / cache headers for future VPS (nginx)
# GitHub Pages cannot set these; enable via Cloudflare Transform Rules
# or paste into server block when migrating off Pages.

# add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
# add_header X-Content-Type-Options "nosniff" always;
# add_header X-Frame-Options "SAMEORIGIN" always;
# add_header Referrer-Policy "strict-origin-when-cross-origin" always;
# add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
# # Tighten CSP after inventory of VK/Metrika/GA/fonts:
# # add_header Content-Security-Policy "default-src 'self'; ..." always;

# location ~* \.(css|js|jpg|jpeg|png|webp|avif|woff2)$ {
#   expires 1y;
#   add_header Cache-Control "public, max-age=31536000, immutable";
# }
